<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jhta.mybatis.mapper.mschedule">
	<select id="getinfo" parameterType="int" resultType="mschedule">
		select * from mschedule where mScheduleNum=#{mScheduleNum}
	</select>

	<select id="getList" parameterType="hashmap"
		resultType="mschduleList">
		select t.branchNum,p.purchasefilmnum, p.purchasedate, p.filmdeadline,
		f.filmName,f.filmNum
		from theather t,purchasefilm p, film f
		where t.branchNum=p.branchNum and p.filmNum=f.filmNum;
	</select>
</mapper>