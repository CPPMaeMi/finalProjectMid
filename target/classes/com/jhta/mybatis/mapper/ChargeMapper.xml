<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jhta.mybatis.mapper.charge">
	<select id="getinfo" parameterType="int" resultType="charge">
		select * from charge where chargeNum=#{chargeNum} 
	</select>
	<select id="genderList" resultType="charge" parameterType="hashmap">
       select sum(c.totalbill),to_char(c.chregdate,'yyyy-mm') chregdate,m.birth birth
       from charge c,membership m
       where TO_CHAR(c.chregdate,'yyyy-mm') BETWEEN #{date} AND #{date1}
       group by to_char(c.chregdate,'yyyy-mm'),m.birth
	</select>
</mapper>